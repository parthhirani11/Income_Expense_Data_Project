<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/
bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="/css/style.css" />
<title>Income Expense App</title>


<header class="navbar navbar-dark sticky-top bg-dark flex-md-nowrap p-0 shadow ">
 
   <a class="navbar-brand col-md-3 col-lg-2 me-0 px-3  fs-6"><h2>Code Decorator</h2></a>
  


  <!-- Navbar on small screens -->
  
    <nav>  
        <div >
                <% if (currentUser) { %>
                <span class="me-2 red-text">Hello, <%= currentUser.name %></span>
                <a class="btn btn-sm btn-outline-secondary red-text btn-red  text-btn" href="/logout">Logout</a>
                <% } %>
          
        </div>
    </nav>
        
</header>

 <style>
  .header {
  width: 100%;
  background: #007bff;
  padding: 15px 25px;
}

.nav-left {
  display: flex;
  gap: 20px; /* space between links */
}

.nav-left a {
  color: white;
  text-decoration: none;
  font-size: 18px;
  font-weight: 600;
}

.nav-left a:hover {
  text-decoration: underline;
}
body,h1,h2,h3,h4,h5,h6 {font-family: "Lato", sans-serif;}
body, html {
  height: 100%;
  color: #777;
  line-height: 1.8;
  min-height: 100vh;
}

/* Create a Parallax Effect */
.bgimg-1, .bgimg-2, .bgimg-3 {
  background-attachment: fixed;
  background-position: center;
  background-repeat: no-repeat;
  background-size: cover;
}

/* First image (Logo. Full height) */
.bgimg-1 {
  background-image: url('./images/coco.jpg');
  min-height: 100%;
}

/* Second image (Portfolio) */
.bgimg-2 {
  background-image: url("./images/1.jpg");
  min-height: 100px;
}

/* Third image (Contact) */
.bgimg-3 {
  background-image: url("./images/R.jpg");
  min-height: 100px;
}

.w3-wide {letter-spacing: 10px;}
.w3-hover-opacity {cursor: pointer;}

/* Turn off parallax scrolling for tablets and phones */
@media only screen and (max-device-width: 1600px) {
  .bgimg-1, .bgimg-2, .bgimg-3 {
    background-attachment: scroll;
    min-height: 400px;
  }
}
</style>
<style>
    .text-btn {
  margin-left: px;   /* Space on the left side of element */
  margin-right: 100px;  /* Space on the right side of element */
}
   .text-box {
  margin-left: 10px;   /* Space on the left side of element */
  margin-right: 700px;
   text-align-last: left;  /* Space on the right side of element */
}
  .text-boxs {
  margin-left: 10px;   /* Space on the left side of element */
  margin-right: 20px;
   text-align-last: left;  /* Space on the right side of element */
}
    .btn-red {
  border: 2px solid rgb(245, 239, 239);    /* Border color and thickness */
  background-color: rgb(50, 46, 46);  /* Optional */
  /* color: red;               Text color */
  padding: 10px 20px;
  border-radius: 6px;
  cursor: pointer;
}
.red-text {
  color: rgb(243, 239, 239);        /* Text color */
}

.green-text {
  color: green;
}

.blue-text {
  color: #007bff;   /* Using hex color */
}
.search-box {
  display: flex;
  justify-content: center;
  align-items: center;
  background: #ffffff;
  width: 300px;
  padding: 8px;
  border-radius: 50px;
  box-shadow: 0px 5px 15px rgba(0,0,0,0.15);
  margin: 40px auto;
}

/* input design */
.search-input {
  width: 50%;
  height: 30px;
  border: none;
  outline: none;
  padding: 0 18px;
  font-size: 17px;
  border-radius: 50px;
  background: transparent;
}

/* button design */
.search-btn {
  height: 30px;
  padding: 0 60px;
  border: none;
  outline: none;
  background: #007bff;
  color: #fff;
  font-size: 17px;
  font-weight: 200;
  cursor: pointer;
  border-radius: 50px;
  transition: 0.3s;
}

/* hover effect */
.search-btn:hover {
  background: white;
  transform: translateY(-2px);
}</style>

